meta {
  name: Mis asignados (tecnico)
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/tickets/asignados?page=0&size=10
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{tokenTecnico}}
}

tests {
  test("responde 200 con una pagina", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().content).to.be.an("array");
  });
}
